{
    // The order of players is important here (leader needs to be first or last, not sure). If in the wrong order, it doesn't work.
    "players": [
        {% for player in item.players %}
        {
            "roomName": "{{player}}"
        },
        {% endfor %}
    ],
    "state": "paused"
}